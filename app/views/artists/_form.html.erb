<h1><%= @artist.id ? 'Edit Artist' : 'Add Artist' %></h1>

<%= form_for [@billboard,@artist] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true %>
  <h1>HERE</h1>
  <%= f.submit %>
<% end %>